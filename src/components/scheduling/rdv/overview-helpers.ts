"use client";

/** Ré-exports pratiques pour les écrans (évite les imports croisés). */
export { dayLabel, dayLong, durationOf, hhmm, displayStatus, getAppUrlClient } from "./shared";
export { rvColor as rvColorOfHost } from "./atoms";
